# 🚀 Quick Start - See the Frontend

## Option 1: Simple Start (Recommended)

```bash
cd /Users/gonzalobam/ranchlink/apps/web
npm install
npm run dev
```

Then open: **http://localhost:3000**

## Option 2: Use the Script

```bash
cd /Users/gonzalobam/ranchlink
./START_SERVER.sh
```

## ⚠️ If You Get Node Version Errors

Your Node.js version might be too old. You need Node 18+.

**Check your Node version:**
```bash
node --version
```

**If it's below 18, update Node:**
```bash
# Option 1: Using Homebrew (Mac)
brew install node@18

# Option 2: Download from nodejs.org
# Visit: https://nodejs.org/
# Download and install Node 18 LTS
```

## 📱 What You'll See

Once the server starts, open these URLs:

1. **Homepage**: http://localhost:3000/
2. **Claim Flow**: http://localhost:3000/start
3. **Animal Card**: http://localhost:3000/a?id=AUS0001
4. **Dashboard**: http://localhost:3000/dashboard
5. **Marketplace**: http://localhost:3000/market
6. **Super Admin**: http://localhost:3000/superadmin
7. **QR Generator**: http://localhost:3000/superadmin/qr-generator

## 🎯 Quick Test

1. Run: `cd /Users/gonzalobam/ranchlink/apps/web && npm install`
2. Run: `npm run dev`
3. Open: http://localhost:3000
4. Click around and explore!

## ✅ Success!

You should see the RanchLink homepage with:
- Hero section
- How it works
- Pricing cards
- Navigation menu

Ready to explore! 🎉


